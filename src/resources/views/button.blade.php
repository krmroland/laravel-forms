<div class="form-group">
  
  <button type="{{ $button->type }}" class="btn btn-primary  {{ $button->class }}">
      <i class="fa {{ $button->icon }}"></i>
      {{ $button->text }}
  </button>
  
</div>
